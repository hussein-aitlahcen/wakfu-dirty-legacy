package com.akfu

import com.ankamagames.wakfu.common.datas.CharacterSerializer.SerializationType

object Test {
  def main(args: Array[String]): Unit = {
    println(List((0, -5)) diff List((0, -5)))
  }
}